@model IEnumerable<StriderCupRacing.Models.Rider>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            FirstName
        </th>
        <th>
            LastName
        </th>
        <th>
            City
        </th>
        <th>
            Country
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RiderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RiderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RiderId })
        </td>
        <td>
			@item.FirstName
        </td>
        <td>
			@item.LastName
        </td>
        <td>
			@item.City
        </td>
        <td>
			@item.Country
        </td>
    </tr>
}

</table>


